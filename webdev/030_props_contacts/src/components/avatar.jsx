import React from 'react';
import Contacto from '../data/infocontactos'
function Avatar(props) {
    return 
    {contactos.map((contacto) => (
        <Contacto
              <img className='circle-img' src={contacto.props.imgURL} alt={props.name} />
        />
      ))}

}